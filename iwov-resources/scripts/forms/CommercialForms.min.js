"use strict";

(function () {
  function r(e, n, t) {
    function o(i, f) {
      if (!n[i]) {
        if (!e[i]) {
          var c = "function" == typeof require && require;
          if (!f && c) return c(i, !0);
          if (u) return u(i, !0);
          var a = new Error("Cannot find module '" + i + "'");
          throw a.code = "MODULE_NOT_FOUND", a;
        }

        var p = n[i] = {
          exports: {}
        };
        e[i][0].call(p.exports, function (r) {
          var n = e[i][1][r];
          return o(n || r);
        }, p, p.exports, r, e, n, t);
      }

      return n[i].exports;
    }

    for (var u = "function" == typeof require && require, i = 0; i < t.length; i++) {
      o(t[i]);
    }

    return o;
  }

  return r;
})()({
  1: [ function (require, module, exports) {
 
	function initFormsComponent() {
      document.forms['dcom-c-forms-component'].setAttribute('novalidate', true);
       if(document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'] != null){
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].parentElement.parentElement.classList.add('cbds-u-display__none');
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].setAttribute('aria-invalid',false);
	   } 
    }

    initFormsComponent();
  
	//====
	// capability of interest
	//====
	document.forms['dcom-c-forms-component']['capability_of_interest'].addEventListener('blur', function (event) {
		 if (!event.target.value) {
            showErrorinpage(event.target); // empty showErrorinpage method is defined in formrenderer.js
            } else {
            hideErrorinpage(event.target); // not empty hideErrorinpage method is defined in formrenderer.js
            }
	});
	//====
    // show capability of interest - others
    // only displayed if capability of interest has 'Other' selected
    //====
    document.forms['dcom-c-forms-component']['capability_of_interest'].addEventListener('change', function (event) {
      var dropdownFieldname="";
      if (event.target.value === 'Other (specify)') { 
        dropdownFieldname = event.target.name; 
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].parentElement.parentElement.classList.remove('cbds-u-display__none');
         document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].focus().trim(); 
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].name=dropdownFieldname;
        //====
		// dcom-capability_of_interest_other_interest
		// only displayed if dcom-capability_of_interest has 'Other (specify)' selected
		//====                                  
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].addEventListener('blur', function (event) {
            if (!event.target.value) {
            showErrorinpage(event.target); // empty showErrorinpage method is defined in formrenderer.js
            } else {
            hideErrorinpage(event.target); // not empty hideErrorinpage method is defined in formrenderer.js
            }
        });
      } else {
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].parentElement.parentElement.classList.add('cbds-u-display__none');
        document.forms['dcom-c-forms-component']['capability_of_interest_other_interest'].setAttribute('aria-invalid',false);
      }
    });
	
   
  }, {}]
}, {}, [1]);